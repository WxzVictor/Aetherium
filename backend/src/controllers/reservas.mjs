export const crearReserva = (req, res) => {
    //Aqui hay que guardar el vuelo y los datos del usuario en la base de datos
};

export const obtenerReservasUsuario = (req, res) => {
    //Coger las reservas del usuario ha iniciado sesion
};